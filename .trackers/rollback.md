# Rollback

**Branch tip after scroll fix:** see latest commit on `deploy/onboarding-combined-test`  
**Prior:** `43e8c94` (trackers resync) / `e603129` (plans UI + trial enum)

```bash
git revert <scroll-fix-sha>   # or reset with user approval
```
CSS-only; no data migration.
