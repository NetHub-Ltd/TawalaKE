# Rollback — Core V2

Core lives only on `core/v2`. Product `main`/`dev` are unaffected.

## Rollback Core work
- Delete or archive remote branch `core/v2`
- Close GitHub Project "Tawala Core V2" if desired

## Do not
- Merge core/v2 into main to "undo" — that would pollute product
