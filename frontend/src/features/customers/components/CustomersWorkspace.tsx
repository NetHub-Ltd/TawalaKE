/**
 * @deprecated Use CustomersList for the overview route.
 * Kept so any residual imports resolve during the IA migration.
 */
export { CustomersList as CustomersWorkspace } from "./CustomersList";
export type { CustomerRow, CustomerDetail } from "@/features/customers/types";
